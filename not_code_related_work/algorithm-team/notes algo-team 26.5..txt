Notizen Algo-Team:

# Load current wave block of speaker sp with speaker_blocksize (fft_blocksize-hrtf_blocksize+1)

sp_block_dict[sp], error_list[sp] = alf.get_sp_block_dict(signal_dict[sp], wave_blockbeginend_dict[sp], sp_blocksize, error_list[sp])

line 121: heap_binaural_dict initialisieren und für realt-time-ausgabe hinten aus mit abspielen; evtl. generator… und dann wie ausgeben?

- springt er bei play von selbst weiter???
- sp_block_dict = Ein Blok zu jedem Speaker
- error_list --> raushauen
- singal dict sp --> raushauen
- wave_blockbeginend_dict ? erster, letzter block
- Beim Ende des signals, setze continue_output[sp] = False (mit try)

